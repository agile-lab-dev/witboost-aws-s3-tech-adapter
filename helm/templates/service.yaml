apiVersion: v1
kind: Service
metadata:
  name: {{ template "s3-tech-adapter.name" . }}
  labels:
{{- include "s3-tech-adapter.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "s3-tech-adapter.name" . }}
